version: '3.8'

services:
  bmi-zscore-who-calculator:
    build: .
    container_name: bmi-zscore-who-calculator
    ports:
      - "8000:8000"
    environment:
      - AUDIT_SECRET_KEY=bmi-zscore-who-calculator-prod-audit-key-2026
      - MODEL_PROVIDER=mock
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
